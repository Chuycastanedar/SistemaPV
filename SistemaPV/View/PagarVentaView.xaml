<Window x:Class="SistemaPV.View.PagarVentaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Proceder Venta" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Seleccione el método de pago:" FontWeight="Bold" Margin="0,0,0,10"/>

        <RadioButton Grid.Row="1" Grid.Column="0" Content="Efectivo" GroupName="MetodoPago" IsChecked="{Binding IsEfectivoSelected}" Margin="0,5,10,5"/>
        <RadioButton Grid.Row="1" Grid.Column="1" Content="Pago con Tarjeta" GroupName="MetodoPago" IsChecked="{Binding IsTarjetaSelected}" Margin="0,5,0,5"/>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10,0,10" BorderThickness="1" BorderBrush="LightGray" Padding="10"
                Visibility="{Binding EfectivoVisibility}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Total : " FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalVenta, StringFormat=F2}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Pagó con : " VerticalAlignment="Center" Margin="0,10,5,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MontoPagado, UpdateSourceTrigger=PropertyChanged}" Width="100" HorizontalAlignment="Left" Margin="0,10,0,0"
                         PreviewTextInput="DecimalValidationTextBox"
                         GotFocus="TextBox_GotFocus"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Devolver : " FontWeight="Bold" VerticalAlignment="Center" Margin="0,10,5,0"/>
                <Border Grid.Row="2" Grid.Column="1" BorderThickness="1" BorderBrush="Gray" Padding="5" Background="#FFF0F0F0" HorizontalAlignment="Left" Width="100" Margin="0,10,0,0">
                    <TextBlock Text="{Binding Cambio, StringFormat=F2}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="80" Margin="0,0,10,0" Command="{Binding CancelarCommand}"/>
            <Button Content="Finalizar" Width="80" IsDefault="True" Command="{Binding FinalizarCommand}"/>
        </StackPanel>
    </Grid>
</Window>